/*input

*/
#include<bits/stdc++.h>
using namespace std;
#define ll long long int
int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    int n,t;
    string a,b;
    cin >> n >> t >> a >> b;
    sort(a.begin(),a.end());
    sort(b.begin(),b.end());
    string ans;
    int w=0;
    for(int )
    return 0;
}